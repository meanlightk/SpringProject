<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<security:http auto-config="true" use-expressions="true">
			<!-- 
			<security:intercept-url pattern="/member/join.do" access="permitAll"/>
			<security:intercept-url pattern="/member/main.do" access="permitAll"/>
			<security:intercept-url pattern="/member/*.do" access="hasRole('ROLE_USER')"/>
			<security:intercept-url pattern="/board/*.do" access="hasRole('ROLE_USER')"/>
			<security:intercept-url pattern="/admin/*.do" access="hasRole('ROLE_USER')"/>
			<security:intercept-url pattern="/main/*.do" access="hasRole('ROLE_USER')"/>
			 -->
		 
 			<security:intercept-url pattern="/join" access="permitAll"/>
		 
		<security:form-login username-parameter="id"
		            password-parameter="pwd"
		            login-page="/member/login"
		            default-target-url="/"
		            login-processing-url="/member/login.do"
		            authentication-failure-url="/member/main.do?fail=true" />
		 
		 <!-- 
		 		<security:form-login username-parameter="id"
		            password-parameter="pwd"
		            login-page="/login"
		            default-target-url="/main/main.do"
		            login-processing-url="/member/login.do"
		            authentication-failure-url="/member/main.do?fail=true" />
		 
		 
		<security:logout logout-url="/member/logout.do"
		        invalidate-session="true"
		/>
 		 -->
	</security:http>        		 
 	
	<security:authentication-manager>
	    
	   <!--  <security:authentication-provider ref="customAuthenticationProvider"/> -->
	    
	</security:authentication-manager>	
    <!-- 
    <bean id="customAuthenticationProvider" class="org.zerock.security.CustomAuthenticationProvider"/>  
     -->
       
</beans>
