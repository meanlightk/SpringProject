<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper

 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.MemberMapper">

	<select id="selectMemberaslogin" parameterType="org.zerock.domain.Member" resultType="org.zerock.domain.Member">
		select
		mem_id as id,
		name,
		pwd,
		pnum,
		email,
		grade,
		regidate,
		updatedate,
		status
		from member where mem_id=#{id} and pwd=#{pwd}
	</select>
	
	<select id="selectMember" resultType="org.zerock.domain.Member">
		select
		mem_id as id,
		name,
		pwd,
		pnum,
		email,
		grade,
		regidate,
		updatedate,
		status
		from member where mem_id=#{id}
	</select>
	
	<insert id="insertMember" parameterType="org.zerock.domain.Member">
		insert into member(mem_id, name, pwd, pnum, email)
		values(#{id}, #{name}, #{pwd}, #{pnum}, #{email})
	</insert>
	
	<update id="updateMember" parameterType="org.zerock.domain.Member">
		update member set pwd=#{pwd}, pnum=#{pnum}, email=#{email} where mem_id=#{id}
	</update>
	
	<delete id="deleteMember">
		delete from member where mem_id=#{id}
	</delete>

</mapper>